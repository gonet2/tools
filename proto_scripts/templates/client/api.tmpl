using UnityEngine;
using System;
using System.Collections.Generic;

namespace NetProto.Api
{
    public enum ENetMsgId
    { {{range .}}
        {{.Name}} = {{.PacketType}}, // {{.Desc}}{{end}}
    };
}
